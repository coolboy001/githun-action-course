name: first workflow

on: push

jobs:
  echo-hello:
    runs-on: ubunntu-lstest
    steps:
      - name: say hello
        run: echo "hello"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: successful steps
        run: |
          echo "I will fail"
          exit 0
      - name: Good Bye
        run: echo "GoodBye"
# jobs run by default parllel
# steps will executed sequntailly and if first step will fail then second step will not executed
